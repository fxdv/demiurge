[package]
name = "demiurge-control"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "KV reservation ledger for Demiurge decode pool admission."

[dependencies]
demiurge-cost = { path = "../demiurge-cost" }
