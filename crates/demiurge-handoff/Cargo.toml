[package]
name = "demiurge-handoff"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "KV cache hand-off descriptors and registry for Demiurge prefill→decode."

[dependencies]
demiurge-cost = { path = "../demiurge-cost" }